<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="AppTheme" parent="@style/AppThemeBase">
        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- specify enter and exit transitions -->
        <item name="android:windowEnterTransition">@transition/details_enter_transition</item>
        <item name="android:windowExitTransition">@transition/details_exit_transition</item>
    </style>

</resources>